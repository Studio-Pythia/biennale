# Pavilion Data Quality Report

Records scanned: **97**
Issues found: **72**

## High severity (1)

- **Venezuela (VE)** · `funding` · No public or private funding sources listed.  
  Fix: Add at least one funding source or explain non-disclosure in notes.

## Medium severity (71)

- **Egypt (EG)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Hungary (HU)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Romania (RO)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Switzerland (CH)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Uruguay (UY)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Venezuela (VE)** · `budget` · Budget amount present but total_budget_disclosed is false.  
  Fix: Set total_budget_disclosed=true or remove amount if uncertain.
- **Venezuela (VE)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Albania (AL)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Argentina (AR)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Armenia (AM)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Azerbaijan (AZ)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Chile (CL)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **India (IN)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Ireland (IE)** · `budget` · Budget amount present but total_budget_disclosed is false.  
  Fix: Set total_budget_disclosed=true or remove amount if uncertain.
- **Ireland (IE)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Italy (IT)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Latvia (LV)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Lebanon (LB)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Luxembourg (LU)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Malta (MT)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Mexico (MX)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Morocco (MA)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Oman (OM)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Panama (PA)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Peru (PE)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Philippines (PH)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Singapore (SG)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Slovenia (SI)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Timor-Leste (TL)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Türkiye (TR)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Ukraine (UA)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Uzbekistan (UZ)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Bosnia and Herzegovina (BA)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Bulgaria (BG)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Cameroon (CM)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Croatia (HR)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Cuba (CU)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Cyprus (CY)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Democratic Republic of the Congo (CD)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **El Salvador (SV)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Equatorial Guinea (GQ)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Estonia (EE)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Ethiopia (ET)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Georgia (GE)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Guatemala (GT)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Guinea (GN)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Haiti (HT)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Iceland (IS)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Indonesia (ID)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Kazakhstan (KZ)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Kosovo (XK)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Kyrgyz Republic (KG)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Lithuania (LT)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Moldova (MD)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Mongolia (MN)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Montenegro (ME)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Nauru (NR)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **New Zealand / Aotearoa (NZ)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **North Macedonia (MK)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Pakistan (PK)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Portugal (PT)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **San Marino (SM)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Senegal (SN)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Sierra Leone (SL)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Somalia (SO)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Syrian Arab Republic (SY)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Tanzania (TZ)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **The Bahamas (BS)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Uganda (UG)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Vietnam (VN)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).
- **Zimbabwe (ZW)** · `sources` · Fewer than 2 source links.  
  Fix: Add at least two corroborating sources (official + independent).

## Low severity (0)

